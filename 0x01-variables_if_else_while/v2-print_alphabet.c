#include <time.h>
#include <stdlib.h>
#include <stdio.h>

/**
 * nain - Entry point
 * Return: Always 0 (Success!)
 */

int main(void)
{
	char low;

		for (low = 'a'; low <= 'z'; low++)
			putchar(low);
		putchar('\n');
		return (0);
}

